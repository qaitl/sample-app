app:
  host: "0.0.0.0"
  port: 8000
  debug: false
  log_level: "INFO"
  loki_url: "http://localhost:3100/loki/api/v1/push"  # vide "" pour désactiver

storage:
  backend: "memory"       # Options: "memory" | "sqlite" | "postgresql"
  sqlite_path: "./todos.db"
  database_url: ""
