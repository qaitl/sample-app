server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # --- Mode dev local : app hors Docker, logs redirigés vers logs/app.log ---
  - job_name: "todo-app-file"
    static_configs:
      - targets: ["localhost"]
        labels:
          job: "todo-app"
          container: "sample-app"
          env: "dev"
          __path__: /logs/app.log

  # --- Mode production Docker : app dans un container ---
  - job_name: "todo-app-docker"
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
        filters:
          - name: name
            values: ["sample-app"]
    relabel_configs:
      - source_labels: ["__meta_docker_container_name"]
        target_label: container
      - source_labels: ["__meta_docker_container_log_stream"]
        target_label: stream
    pipeline_stages:
      - json:
          expressions:
            level: level
            event: event
            request_id: request_id
      - labels:
          level:
          request_id:
